[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[tool.black]
line_length = 120

[tool.ruff]
line-length = 120
# pyflakes, pycodestyle, isort
select = ["F", "E", "W", "I001"]